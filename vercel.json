{
  "buildCommand": "npx prisma generate && npx prisma migrate deploy --force && npm run build",
  "outputDirectory": "public",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "remix"
}
