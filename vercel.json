{
  "buildCommand": "npx prisma generate && node setup-db.js && node test-db-connection.js && npm run build",
  "outputDirectory": "public",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "remix",
  "env": {
    "NODE_ENV": "production"
  }
}
